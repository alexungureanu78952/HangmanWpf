<Window x:Class="HangmanWpf.Views.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hangman - Game"
        Height="700"
        Width="900"
        Background="{DynamicResource PrimaryBackground}"
        Foreground="{DynamicResource Foreground}"
        WindowStartupLocation="CenterScreen">
        <DockPanel>
                <!-- Menu Bar -->
                <Menu DockPanel.Dock="Top"
                      Background="{DynamicResource SecondaryBackground}"
                      Foreground="{DynamicResource Foreground}">
                        <MenuItem Header="File">
                                <MenuItem Header="New Game"
                                          Command="{Binding StartNewGameCommand}"/>
                                <MenuItem Header="Open Game"
                                          Command="{Binding LoadGameCommand}"/>
                                <MenuItem Header="Save Game"
                                          Command="{Binding SaveGameCommand}"/>
                                <MenuItem Header="Statistics"
                                          Command="{Binding ShowStatisticsCommand}"/>
                                <Separator/>
                                <MenuItem Header="Cancel"
                                          Command="{Binding CancelCommand}"/>
                        </MenuItem>
                        <MenuItem Header="Category">
                                <MenuItem Header="Movies"
                                          Command="{Binding ChangeCategoryCommand}"
                                          CommandParameter="Movies"/>
                                <MenuItem Header="Cars"
                                          Command="{Binding ChangeCategoryCommand}"
                                          CommandParameter="Cars"/>
                                <MenuItem Header="Rivers"
                                          Command="{Binding ChangeCategoryCommand}"
                                          CommandParameter="Rivers"/>
                                <MenuItem Header="Mountains"
                                          Command="{Binding ChangeCategoryCommand}"
                                          CommandParameter="Mountains"/>
                                <MenuItem Header="States"
                                          Command="{Binding ChangeCategoryCommand}"
                                          CommandParameter="States"/>
                                <MenuItem Header="Animals"
                                          Command="{Binding ChangeCategoryCommand}"
                                          CommandParameter="Animals"/>
                                <MenuItem Header="Countries"
                                          Command="{Binding ChangeCategoryCommand}"
                                          CommandParameter="Countries"/>
                                <MenuItem Header="All Categories"
                                          Command="{Binding ChangeCategoryCommand}"
                                          CommandParameter="All Categories"/>
                        </MenuItem>
                        <MenuItem Header="Theme">
                                <MenuItem Header="Dark Purple"
                                          Command="{Binding ChangeThemeCommand}"
                                          CommandParameter="DarkPurple"/>
                                <MenuItem Header="Dark Red"
                                          Command="{Binding ChangeThemeCommand}"
                                          CommandParameter="DarkRed"/>
                        </MenuItem>
                        <MenuItem Header="Help">
                                <MenuItem Header="About"
                                          Command="{Binding ShowAboutCommand}"/>
                        </MenuItem>
                </Menu>

                <!-- Main Content -->
                <Grid Margin="20">
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Panel: User Info & Hangman -->
                        <StackPanel Grid.Column="0"
                                    Margin="0,0,20,0">
                                <!-- User Info -->
                                <TextBlock Text="{Binding CurrentUser.Username}"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource Accent}"
                                           Margin="0,0,0,10"/>
                                <Image Source="{Binding CurrentUser.ImagePath}"
                                       Width="80"
                                       Height="80"
                                       Margin="0,0,0,20"/>

                                <!-- Hangman Art -->
                                <TextBlock Text="{Binding HangmanDisplay}"
                                           FontFamily="Courier New"
                                           FontSize="12"
                                           Foreground="{DynamicResource Foreground}"
                                           Margin="0,0,0,20"/>

                                <!-- Stats -->
                                <Border Background="{DynamicResource SecondaryBackground}"
                                        Padding="10">
                                        <StackPanel>
                                                <TextBlock Text="{Binding StringFormat='Level: {0}/3', Path=Level}"
                                                           Foreground="{DynamicResource Accent}"
                                                           Margin="0,0,0,5"/>
                                                <TextBlock Text="{Binding StringFormat='Wrong: {0}/6', Path=WrongCount}"
                                                           Foreground="{DynamicResource Error}"
                                                           Margin="0,0,0,5"/>
                                                <TextBlock Text="{Binding StringFormat='Timer: {0}s', Path=TimerSeconds}"
                                                           Foreground="{DynamicResource Info}"/>
                                        </StackPanel>
                                </Border>
                        </StackPanel>

                        <!-- Center Panel: Word & Buttons -->
                        <StackPanel Grid.Column="1">
                                <!-- Word Display -->
                                <TextBlock Text="{Binding WordDisplay}"
                                           FontSize="48"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource Accent}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,30"
                                           Padding="20"
                                           Background="{DynamicResource SecondaryBackground}"/>

                                <!-- Status Message -->
                                <TextBlock Text="{Binding StatusMessage}"
                                           FontSize="14"
                                           Foreground="{DynamicResource Success}"
                                           HorizontalAlignment="Center"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,20"
                                           Height="40"/>

                                <!-- Letter Tiles -->
                                <ItemsControl ItemsSource="{Binding LetterTiles}"
                                              Margin="0,0,0,10">
                                        <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                        <UniformGrid Columns="13"/>
                                                </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                        <Button Content="{Binding Letter}"
                                                                Command="{Binding DataContext.GuessLetterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                CommandParameter="{Binding Letter}"
                                                                IsHitTestVisible="{Binding IsClickable}"
                                                                Focusable="False"
                                                                Margin="2"
                                                                MinWidth="28"
                                                                MinHeight="28"
                                                                Padding="4"
                                                                BorderThickness="1"
                                                                BorderBrush="{DynamicResource Border}"
                                                                Foreground="{DynamicResource Foreground}">
                                                                <Button.Style>
                                                                        <Style TargetType="Button">
                                                                                <Setter Property="Background"
                                                                                        Value="{DynamicResource SecondaryBackground}"/>
                                                                                <Setter Property="FontWeight"
                                                                                        Value="Bold"/>
                                                                                <Setter Property="Opacity"
                                                                                        Value="1"/>
                                                                                <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding State}"
                                                                                                     Value="Correct">
                                                                                                <Setter Property="Background"
                                                                                                        Value="{DynamicResource Success}"/>
                                                                                        </DataTrigger>
                                                                                        <DataTrigger Binding="{Binding State}"
                                                                                                     Value="Wrong">
                                                                                                <Setter Property="Background"
                                                                                                        Value="{DynamicResource Error}"/>
                                                                                        </DataTrigger>
                                                                                        <DataTrigger Binding="{Binding IsClickable}"
                                                                                                     Value="False">
                                                                                                <Setter Property="Opacity"
                                                                                                        Value="0.9"/>
                                                                                        </DataTrigger>
                                                                                </Style.Triggers>
                                                                        </Style>
                                                                </Button.Style>
                                                        </Button>
                                                </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Center">
                                        <Button Content="New Game"
                                                Command="{Binding StartNewGameCommand}"
                                                Margin="2"
                                                Width="160"/>
                                        <Button Content="Exit"
                                                Command="{Binding CancelCommand}"
                                                Margin="2"
                                                Width="160"/>
                                </StackPanel>
                        </StackPanel>

                        <!-- Right Panel: Category Info -->
                        <StackPanel Grid.Column="2"
                                    Margin="20,0,0,0">
                                <TextBlock Text="Category"
                                           FontSize="14"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource Accent}"
                                           Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding Category}"
                                           FontSize="16"
                                           Foreground="{DynamicResource Foreground}"
                                           Padding="10"
                                           Background="{DynamicResource SecondaryBackground}"
                                           TextWrapping="Wrap"/>
                        </StackPanel>
                </Grid>
        </DockPanel>
</Window>
